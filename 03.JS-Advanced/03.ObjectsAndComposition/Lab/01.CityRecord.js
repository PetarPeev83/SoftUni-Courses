function cityRecord(name, population, treasury) {

    let cityObj = {
        name,
        population,
        treasury,
    };
    return cityObj;
// console.log(cityObj);
}
cityRecord('Tortuga',
    7000,
    15000);
// cityRecord('Santo Domingo',
// 12000,
// 23500);